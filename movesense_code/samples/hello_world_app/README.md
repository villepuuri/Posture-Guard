# "Hello World" app (hello_world_app)

This sample demonstrates a creation of a simple service (= ResourceProvider) that implements a classic "Hello World" operation.

## What it does:

* Provides a GET & SUBSCRIBE resource that sends "Hello World" -message

## APIs defined

### /Sample/HelloWorld \[GET, SUBSCRIBE\]

Returns or gives notifications with a greeting.
